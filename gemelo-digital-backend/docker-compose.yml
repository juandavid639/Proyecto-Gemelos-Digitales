services:
  gemelo-backend:
    build:
      context: ..
      dockerfile: gemelo-digital-backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./config:/app/config